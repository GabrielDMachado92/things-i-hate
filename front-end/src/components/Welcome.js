import React from "react";
import { Container } from "react-bootstrap";

const Welcome = ({ title }) => {
  return (
    <Container>
      <h1>You don't hate anything, congrats!</h1>
    </Container>
  );
};
export default Welcome;
